/// <reference types="cypress" />
// TEST COUNT: 0
describe('Muation: createCategory', () => {
    let id = '';
    const standardMutationBody = `
        code
        message
        error
        category {
            id
        }
    `;

});